---
title: Drafts
description: a private page which contains articles that are not ready yet for publication
tags: [draft, article]
is_hidden: true
---
<% sorted_articles.each do |post| %>
    <% if post[:draft] %>
        <div class="post-list">
            <h2><%= link_to post[:title], post.path %></h2>
            <aside>Posted on <%= get_pretty_date(post) %></aside>

            <article>
                <%= get_post_start(post) %>
            </article>
        </div>
    <% end %>
<% end %>
<p><a href="/archive">&larr; Archive</a></p>
